apiVersion: v1
kind: Service
metadata:
  name: isa-superapp
  labels:
    app: isa-superapp
spec:
  selector:
    app: isa-superapp
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP