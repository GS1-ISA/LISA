[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent_core"
version = "0.0.1"
description = "Core primitives for planner/builder/verifier/critic agents with policy and rewards."
requires-python = ">=3.11"
authors = [{ name = "ISA Agent Team" }]
dependencies = []

