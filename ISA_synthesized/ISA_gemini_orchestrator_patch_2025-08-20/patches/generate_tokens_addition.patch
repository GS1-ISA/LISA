*** a/scripts/generate_tokens.py
--- b/scripts/generate_tokens.py
***************
*** 0 ****
--- 1,40 ----
+ import argparse, os, sys, datetime, pathlib, json
+ 
+ def main():
+     ap = argparse.ArgumentParser()
+     ap.add_argument("--out", required=True, help="Output CSS path (e.g., webui/tokens.css)")
+     args = ap.parse_args()
+ 
+     out_path = pathlib.Path(args.out)
+     out_path.parent.mkdir(parents=True, exist_ok=True)
+ 
+     # TODO: replace with real Figma/design token extraction.
+     tokens = {
+         "color-primary": "#1f6feb",
+         "color-accent": "#5b2be0",
+         "radius-lg": "16px",
+         "spacing-md": "12px",
+     }
+     css_lines = [
+         f"/* tokens.css :: generated {datetime.datetime.utcnow().isoformat()}Z */",
+         ":root {",
+         *[f"  --{k}: {v};" for (k, v) in tokens.items()],
+         "}",
+         "",
+     ]
+     out_path.write_text("\n".join(css_lines), encoding="utf-8")
+     print(f"Wrote {out_path} with {len(tokens)} tokens.")
+ 
+ if __name__ == "__main__":
+     sys.exit(main())
